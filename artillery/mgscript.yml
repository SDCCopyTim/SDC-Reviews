config:
  target: 'http://localhost:3004'
  http:
    pool: 20
  phases:
    - duration: 5
      arrivalRate: 500
scenarios:
  - flow:
    - get:
        url: "/api/date/{{$randomNumber(9000000,10000000)}}"